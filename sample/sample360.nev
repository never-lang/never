
extern "/lib64/libm.so" func sinh(x -> float) -> float;
extern "/lib64/libm.so" func cosh(x -> float, s -> string) -> string;

func main() -> int
{
    var v1 = sinh(3.1414);
    var v2 = cosh(3.1414, "text");

    printf(v1);
    prints(v2);

    0
}

