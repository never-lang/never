
func dir_deg(int d, float degree) -> float
{
    return (d == 0 ? func fah2cel(float f) -> float { return (f - 32.0) / 1.8; }
                   : func cel2fah(float c) -> float { return c * 1.8 + 32.0; })(degree);
}

func main() -> float
{
    return dir_deg(1, 100.0);
}

