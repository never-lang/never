
func dir_deg(d : int, degree : float) -> float
{
    (d == 0 ? let func fah2cel(f : float) -> float { (f - 32.0) / 1.8 }
            : let func cel2fah(c : float) -> float { c * 1.8 + 32.0 })(degree)
}

func main() -> float
{
    dir_deg(1, 100.0)
}

