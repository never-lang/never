/**
 * level expert
 */
func execute(float a, float b, f1(float, float) -> float) -> float
{
    return f1(a, b);
} 
 
func main() -> float
{
    return execute(10, 20, 
                   func min(float a, float b) -> float 
                   { 
                       return a < b ? a : b; 
                   });
}

