/**
 * level expert
 */
func execute(int a, int b, f1(int, int) -> int) -> int
{
    return f1(a, b);
} 
 
func main() -> int
{
    return assert(execute(10, 20, 
                  func min(int a, int b) -> int
                  { 
                       return a < b ? a : b; 
                  }) == 10);
}

