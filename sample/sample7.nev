/**
 * Passing function and calling returned function.
 */
func twice(int c1, int c2) -> (int, int) -> int
{
    func mult() -> int
    {
        return c1 + c2;
    }

    func twice_func(int a, int b) -> int
    {
        /*func twice_func_nest(int a, int b) -> int
        {
            return mult() * single_func(a, b);
        }
        return twice_func_nest(a, b); */
        return mult();
    }
    
    return twice_func;
}

func main() -> int
{
    return twice(2, 3)(10, 20);
}


