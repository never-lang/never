
func pr_int( I[D] : int) -> int
{
    let i = 0;

    for (i = 0; i < D; i = i + 1)
    {
        print(I[i])
    };

    0
}

func main() -> int
{
    let a = [ 0, 1, 2, 3, 4, 5, 6 ] : int;
    let lc = [ x | x in a[ 1 .. 3 ] ] : int;

    pr_int(lc);

    0
}

