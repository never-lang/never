/**
 * Crazy level max
 */
func main() -> int 
{
    assert((2 < 1 ? lambda func min(a -> float, b -> float) -> float { a < b ? a : b }
                  : lambda func max(a -> float, b -> float) -> float { a > b ? a : b })(10.0, 20.0) == 20.0)
}

