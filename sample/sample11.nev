/**
 * Crazy level max
 */
func main() -> int 
{
    return assert((2 < 1 ? func min(a -> float, b -> float) -> float { return a < b ? a : b; }
                         : func max(a -> float, b -> float) -> float { return a > b ? a : b; })(10.0, 20.0) == 20.0);
}

