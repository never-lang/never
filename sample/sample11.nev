/**
 * Crazy level max
 */
func main() -> float
{
    return (2 < 1 ? func min(float a, float b) -> float { return a < b ? a : b; }
                  : func max(float a, float b) -> float { return a > b ? a : b; })(10, 20);
}

