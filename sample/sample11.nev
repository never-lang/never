/**
 * Crazy level max
 */
func main() -> int
{
    return (2 < 1 ? func min(int a, int b) -> int { return a < b ? a : b; }
                  : func max(int a, int b) -> int { return a > b ? a : b; })(10, 20);
}

