
func table(a -> float, b -> float, c -> float) -> float
{
    let p = [ a, b, c, a + b, a + c, b + c ] -> float
    let q = 3

    return p[q];
}

func main() -> float
{
    return table(3.0, 4.0, 5.0);
}

