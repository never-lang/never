
func main() -> int
{
    var t = {[ 10 ]} : (int) -> int;
    
    assert(t[0] == nil);
    assert(nil == t[0]);
    
    print(t[0](10));
    assert(false);

    0
}
catch (nil_pointer)
{
    prints("nil_pointer\n");
    0
}

