
func sub(a -> int, b -> int) -> int
{
    return a - b;
}

func add(a -> int, b -> int) -> int
{
    return a + b;
}

func execute(f(int, int) -> int, a -> int, b -> int) -> int
{
    return f(a, b);
}

func main() -> int 
{
    return execute(add, 12, 10);
}

