
record R { x : int; y : int; }

func pr( a[D] : R ) -> int
{
    for ( i in [ 0 .. D ] )
    {
        prints(a[i].x + " " + a[i].y + "\n")
    }
}

func main() -> int
{
    let a = [ R(x, y) | x in [ 0 .. 5 ]; y in [ 1 .. x + 1 ] ] : R;

    pr(a);

    0    
}
