
record Tree
{
    value -> int;
    left -> Tree;
    right -> Tree;
    print(t -> Tree) -> int;
}

func print_tree(t -> Tree) -> int
{
    prints("tree value = " + t.value + "\n");
    0
}

func main() -> int
{
    var t0 = new Tree(100, nil, nil, print_tree);

    t0.print(t0);

    0
}

