
func three(d : int, c : int) -> int
{
    let t = [ 1, 2, 3 ] : int;

    t[0] = d;
    17770 / d
}

func two(d : int) -> int
{
    three(d, 199);
    assert(false);
    0
}

func one(d : int) -> int
{
    two(d);
    assert(false);
    0
}
catch (division_by_zero)
{
    100 + 20 + 9
}

func main() -> bool
{
    assert(one(0) == 129)
}

