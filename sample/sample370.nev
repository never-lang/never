
func get_char(str : string, i : int, c : char) -> char
{
    print(length(str));

    printc('X');
    printc(c);
    printc(str[i]);

    'Z'
}

func main() -> int
{
    get_char("texttexttex", 2, 'Y');
    
    0
}

