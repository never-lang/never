func no(float n) -> float
{
    return (-3.0) * n;
}

func mutrec(float n) -> float
{
    func A(float n) -> float
    {
        return (n == 1.0) ? B(1.0) : no(n);
    }
    
    func B(float n) -> float
    {
        return (n == 1.0) ? A(11.0) : 12.0;
    }
    
    func C(float n) -> float
    {
        return A(n);
    }
    
    return C(n);
}

func main() -> int
{
    return assert(mutrec(1.0) == -33.0);
}

