func fib(n -> int) -> int 
{
    return func fib_h(n -> int, a -> int, b -> int) -> int
    {
         return (n == 0) ? a : (n == 1) ? b : fib_h(n - 1, b, a + b);
    } (n, 1, 1);
}

func main() -> int
{
    return fib(20);
}

