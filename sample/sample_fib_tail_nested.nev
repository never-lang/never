func fib(n -> int) -> int 
{
    lambda func fib_h(n -> int, a -> int, b -> int) -> int
    {
         (n == 0) ? a : (n == 1) ? b : fib_h(n - 1, b, a + b)
    } (n, 1, 1)
}

func main() -> int
{
    fib(20)
}

