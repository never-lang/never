func calc(n -> int, coeff -> int) -> int
{
     func fib(n -> int) -> int
     {
         return ((n == 0) ? 1 : (n == 1) ? 1 : fib(n - 1) + fib(n - 2)) * coeff;
     }
     
     return fib(n);
}

func main() -> int 
{
     return calc(7, 2);
}

