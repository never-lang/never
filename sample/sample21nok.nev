func calc(n : int, coeff : int) -> int
{
     func fib(n : int) -> int
     {
         ((n == 0) ? 1 : (n == 1) ? 1 : fib(n - 1) + fib(n - 2)) * coeff
     }
     
     fib(n)
}
func main() -> int 
{
     calc(7, 2)
}

