
func test() -> int
{
    var x = 10;
    func n() -> int {
        func nested() -> int {
            let v1 = let func () -> int { x }();
            var x = 20;
            let v2 = let func () -> int { x }();
        
            print(v1);
            print(v2);
        
            assert(v1 == 10);
            assert(v2 == 20);

            0
        };
        nested()
    };
    n();
    
    0
}

func main() -> int
{
    test();

    0
}

