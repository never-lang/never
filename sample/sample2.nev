
func min(int a, int b) -> int
{
    return (a < b) ? a : b;
}

func twice(single_func(int, int) -> int) -> (int, int) -> int
{
    func twice_func(int a, int b) -> int
    {
        func one_more() -> int
        {
            return 1000;
        }
    
        return one_more() * single_func(a, b);
    }

    return twice_func;
}

func main() -> int
{
    return twice(min)(12, 15);
}


