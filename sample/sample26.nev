func no(n -> float) -> float
{
    n
}

func mutrec(n -> float, no(float) -> float) -> float
{
    func A(no(float) -> float, n -> float) -> float
    {
        (n == 1.0) ? B(1.0, no) : 87.0
    }
    
    func B(n -> float, no(float) -> float) -> float
    {
        (n == 1.0) ? A(no, 10.0) : 12.0
    }
    
    func C(n -> float) -> float
    {
        A(no, n)
    }
    
    C(n)
}

func main() -> int 
{
    assert(mutrec(1.0, no) == 87.0)
}

