/**
 * Crazy level max
 */
func min(a : float, b : float) -> float
{ 
    a < b ? a : b
} 
 
func max(a : float, b : float) -> float
{
    a > b ? a : b
}

func main() -> int
{
    assert((1.0 < 2.0 ? min : max)(10.0, 20.0) == 10.0)
}

