/**
 * Crazy level max
 */
func min(float a, float b) -> float
{ 
    return a < b ? a : b;
} 
 
func max(float a, float b) -> float
{
    return a > b ? a : b;
}

func main() -> int
{
    return assert((1.0 < 2.0 ? min : max)(10.0, 20.0) == 10.0);
}

