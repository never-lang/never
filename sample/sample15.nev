/**
 * Crazy level max
 */
func min(float a, float b) -> float
{ 
    return a < b ? a : b;
} 
 
func max(float a, float b) -> float
{
    return a > b ? a : b;
}

func main() -> float
{
    return (1 < 2 ? min : max)(10, 20);
}

