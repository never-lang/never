func min(int a, int b) -> int 
{ 
    return a < b ? a + a : b + b; 
}

func execute(int a, int b, f1(int, int) -> int) -> int
{
    return f1(a, b);
} 
 
func main() -> int
{
    return execute(10, 20, min);
}

