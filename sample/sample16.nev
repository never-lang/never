func min(a -> float, b -> float) -> float 
{ 
    a < b ? a + a : b + b
}

func execute(a -> float, b -> float, f1(float, float) -> float) -> float
{
    f1(a, b)
} 
 
func main() -> int
{
    assert(execute(15.0, 20.0, min) == 30.0)
}

