
record R { a : int; b : int; }

func forincheck( a [ from .. to ] : int ) -> int
{
    let b = [ [1..10], [10..20], [20..30] ] : [..] : range;

    for (x in [1..10])
    {
        for (y in [1..10])
        {
            print(x + y)
        }
    };

    0
}

func r( a[D] : int, s1[from .. to] : int, [from2..to2] : range ) -> [..] : int
{
    let b = [1, 2, 3] : int;

    a [ from .. to ]
}

func main() -> int
{
    0
}
