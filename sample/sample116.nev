
func f1(tab[row, col] : int) -> [D] : let [D,D] : int
{
    [ tab, tab, tab ] : let [D, D] : int
}

func main() -> bool
{
    assert( f1( [ [ 1, 2 ],
                  [ 3, 4 ] ] : int)[0][1,1] == 4 )
}

