
func pr_dim ( dim[D] : int ) -> int
{
    for (i in [0 .. D - 1])
    {
        print(dim[i])
    };

    assert( D == 2 );
    assert( dim[0] == 111);
    assert( dim[1] == 99);

    0
}

func main() -> int
{
    let dim = [110..200, 100..0][1, 1];

    pr_dim(dim);

    0
}
