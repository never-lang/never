
record Rec {
    x -> int;
    y -> int;
}

func test() -> int
{
    var r1 = new Rec(8, 9);
    
    r1 = nil;    
    
    assert(r1 == nil);
    assert(nil == r1);
    
    0
}

func main() -> int
{
    test();

    0
}

