
enum E { one, two, three, four, five, six }
enum F { eight, nine, ten }

func main() -> int
{
    let e = E::six;
            
    match e
    {
        E::one -> { assert(false); 1 };
        E::two -> { assert(false); 2 };
        E::three -> { assert(false); 3 };
        E::four -> { assert(false); 4 };
        E::five -> { assert(false); 5 };
        else -> { assert(true); 6 };
    }
    
}

