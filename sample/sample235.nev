
func three(d : int, c : int) -> int
{
    let t = [ 1, 2, 3 ] : int;

    t[0] = d;
    17770 / d
}

func two(d : int) -> int
{
    three(d, 199);
    assert(false);
    0
}

func one(d : int) -> int
{
    two(d);
    assert(false);
    0
}

func main() -> int
{
    one(0);
    assert(false);
    0
}
catch
{
    0
}

