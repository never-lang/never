
enum Name { Max, Jacob, Other }

func hello(n : Name) -> string
{
    match (n)
    {
        Name.Max -> "Hi Max!\n";
        Name.Jacob -> "Hi Jacob!\n";
        else -> "Hi Unknown!\n";
    }
}

func main() -> int
{
    prints(hello(Name.Max));

    0
}

