/**
 * more crazy find with never
 */ 
func min(int a, int b) -> int
{
    return a < b ? a : b;
}

func max(int a, int b) -> int
{
    return a > b ? a : b;
}

func main() -> int
{
    return ((110 < 100) ? min : max)(10, 20);
}


