/**
 * more crazy find with never
 */ 
func min(a -> float, b -> float) -> float
{
    a < b ? a : b
}

func max(a -> float, b -> float) -> float
{
    a > b ? a : b
}

func main() -> int
{
    assert(((110 != 100) ? min : max)(10.0, 20.0) == 10.0)
}


