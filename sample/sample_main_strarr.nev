
func test1(argv[argc] : string) -> int
{
    for (s in argv)
    {
        prints("****\t" + s + "\n")
    };
    0
}

func test2(argv[D1, D2] : string) -> int
{
    prints(argv[1,0] + "\n");
    0
}

func main(argv[argc] : string) -> int
{
    test1( [] : string );
    test1( argv );
    test2( [ [ "aa" ], [ "bbb" ] ] : string );
    0
}
