
func t1000( a : (int, int) ) -> int
{
    prints("func t1000 " + a[0] + " " + a[1] + "\n");
    10
}

func main() -> int
{
    let p = (10, 10) : (int, int);

    ( p, ) : ( (int, int) ) |> t1000();

    0
}
