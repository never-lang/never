
record Node {
    a -> int;
    f() -> int;
    t -> Tree;
    left -> Node;
};

record Tree {
    value -> int;
    f(p -> int) -> (int) -> int;
    left -> Tree;
    right -> Tree;
};

func count(p -> Tree) -> int
{
    var t = new Tree;
    
    t.right.value
}

func main() -> int
{
    0
}

