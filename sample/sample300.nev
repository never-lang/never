
record Node {
    a -> int;
    f() -> int;
    f -> float;
};

record Tree {
    value -> int;
    f(p -> int) -> (int) -> Tree;
    left(int, Tree) -> (string, Tree) -> Tree;
    left -> Tree;
    right -> Tree;
};

func count(tab[D] -> Tree) -> Tree
{
    new Tree
}

func main() -> int
{
    0
}

