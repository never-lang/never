
record Node {
    a -> int;
    f() -> int;
    t -> Tree;
    i -> Node;
};

record Tree {
    value -> int;
    f(p -> int) -> (int) -> int;
    left -> Tree;
    right -> Node;
    left(int, Tree) -> (string, Tree) -> Tree;
};

func count(p -> Tree) -> Node
{
    var t = new Tree;
    
    t.right.t.right
}

func main() -> int
{
    0
}

