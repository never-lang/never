
func testdo() -> int
{
    var i = 10;
    var sum = 0;
    
    do
    {
        print(i);
        sum = sum + i;
        i = i - 1
    }
    while (i > 0);
    
    assert(sum == 55);
    
    0
}


func main() -> int
{
    testdo()
}

