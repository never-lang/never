
func power(a -> int, n -> int) -> int
{
    func powrec(a -> int, n -> int, val -> int) -> int
    {
        return n == 0 ? val : powrec(a, n - 1, a * val);
    }

    return powrec(a, n, 1);
}

func main() -> int
{
    return power(2, 5);
}

