func f(i -> int) -> (int) -> int
{
    let f1 = lambda func(a -> int) -> int { a * a };
    let f2 = lambda func(b -> int) -> int { b + b };
    let f3 = lambda func(c -> int) -> int { i * i };    
    
    f1 = f3;
    f2 = f3;
    f1
}

func main() -> int
{
    f(5)(20)
}

