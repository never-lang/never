
func area(a -> float, b -> float, c -> float) -> float
{
    let p = (a + b + c) / 2.0
    func coeff() -> float { return 100.0; }
    
    return coeff() * sqrt(p * (p - a) * (p - b) * (p - c));
}

func main() -> float
{
    return area(3.0, 4.0, 5.0);
}

