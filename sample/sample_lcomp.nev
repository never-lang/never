

func decor(str : string) -> string
{
    "###" + str + "###\n"
}

func main() -> int
{
    var i = 0;
    var texts = [ "one", "two", "three" ] : let string;
    var decors = [ decor(txt) | txt in texts ] : let string;

    for (i = 0; i < 3; i = i + 1)
    {
        prints(decors[i])
    };
    
    0
}

