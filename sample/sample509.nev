
enum E { ZERO, ONE, TWO, THREE }

func check(e : int, v : int) -> bool
{
    assert(e == v)
}

func main() -> int
{
    let e = [ E::THREE, E::TWO, E::ONE ] : E;

    check(e[0], 3);
    check(e[1], 2);
    check(e[2], 1);

    0
}
