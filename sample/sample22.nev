func calc(int coeff, int n) -> int
{
    func C() -> int 
    {
        return coeff;
    }

    func fib(int n) -> int 
    {
        return ((n == 0) ? 1 : (n == 1) ? 1 : fib(n - 1) + fib(n - 2)) * C();
    }
     
    return fib(n);
}

func main() -> int 
{
     return calc(2, 7);
}

