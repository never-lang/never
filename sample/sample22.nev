func calc(coeff : int, n : int) -> int
{
    func C() -> int 
    {
        coeff
    }

    func fib(n : int) -> int 
    {
        ((n == 0) ? 1 : (n == 1) ? 1 : fib(n - 1) + fib(n - 2)) * C()
    }
     
    fib(n)
}

func main() -> int 
{
     calc(2, 7)
}

