
enum Optional { Some { value : int; s : int; }, None }

func calc() -> Optional
{
    Optional.Some(10, 1)
}

func main() -> int
{
    let V = 10;
    let c = calc();

    match c
    {
        Optional.Some(x, v) -> print(2 * x + v + V);
        Optional.None -> print(0);
    };
    
    0
}

