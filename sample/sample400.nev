
enum Optional { Some { x : int; y : int; }, None }
enum Optional_S { Some, None }

record Some { x : int; y : int; }

func calc() -> Optional
{
    Optional::Some(10, 1)
}

func main() -> int
{
    let V = 10;
    let c = calc();
    let z = 1000;

    match calc()
    {
        Optional::Some(x, y) -> print(x + y);
        Optional::None -> 30;
    }
    
    /* Some(10, 20).y */
}

