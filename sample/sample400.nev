
enum Optional { Some { x : int; y : int; }, None }

func calc() -> Optional
{
    Optional::Some(10, 1)
}

func main() -> int
{
    let V = 10;
    let c = calc();
    let z = 1000;

    match c
    {
        Optional::Some(x, y) -> print(2 + x * y);
        Optional::None -> print(0);
    };
    
    0
}

