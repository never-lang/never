
func degrees(conv(float) -> float, float degree) -> float
{
    return conv(degree);
}

func main() -> int
{
    return degrees(func rea2cel(float d) -> float
                   {
                        return d * 4.0 / 5.0;
                   }, 100.0);
}

