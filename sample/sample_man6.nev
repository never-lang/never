
func degrees(conv(float) -> float, degree -> float) -> float
{
    conv(degree)
}

func main() -> float
{
    degrees(func rea2cel(d -> float) -> float
            {
                d * 4.0 / 5.0
            }, 100.0)
}

