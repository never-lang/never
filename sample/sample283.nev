
func print_cl(cl[D] : string) -> int
{
    var i = 0;
    
    for (i = 0; i < D; i = i + 1)
    {
        prints(cl[i] + "\n")
    };
    
    0
}

func cl() -> [_] : string
{
    [ "[" + x + "," + y + "]" | x in [1, 2, 3, 4] : int; y in [1, 2, 3, 4, 5] : int ] : string
}
    
func main() -> int
{
   print_cl(cl())
}

